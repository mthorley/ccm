FROM python:3.12-slim

WORKDIR /app
COPY tls_graph_adapter/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
COPY tls_graph_adapter ./tls_graph_adapter

USER nobody
EXPOSE 4318
CMD ["uvicorn", "tls_graph_adapter.main:app", "--host", "0.0.0.0", "--port", "4318"]