FROM python:3.12-slim

WORKDIR /app
COPY ccm_dashboard/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
COPY ccm_dashboard ./ccm_dashboard

USER nobody
EXPOSE 8080
CMD ["uvicorn", "ccm_dashboard.main:app", "--host", "0.0.0.0", "--port", "8080"]
